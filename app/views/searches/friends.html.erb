<% if @friends.length === 0 %>
  <h1>You have no friends on FourSquare.</h1>
<% end %>

<ul>
  <% @friends.each do |friend| %>
    <li><%= "#{friend['firstName']} #{friend['lastName']}" %></li>
  <% end %>
</ul>